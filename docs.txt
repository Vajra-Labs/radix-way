Option 1: Pure Tree Approach (Complex)
- Har route add karte waqt tree traverse karke matching middleware collect karo
- Tree me hi store karo (no separate storage)

Option 2: Hybrid Approach
- #middleware: Map<string, T[]>; // Store wildcard middleware separately

 *      (root, global middleware)
 └─ api/*   (api middleware)
     └─ users/:id   (route middleware + handler)

1. If same method hai in Root/Api/Handler tak, handler se before middleware hoge.
2. If 'ALL' method hai, mean ish ka sabhi par applied hoga, chahite GET,POST, ...etc kyu na ho